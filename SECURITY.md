# Security

Do not commit RPC API keys, private keys, wallet seed phrases, or personal access tokens.

If a network requires an authenticated RPC endpoint, add support through GitHub Actions secrets rather than placing credentials in `config/networks.json`.
