[project]
name = "chain-observatory"
version = "0.2.0"
description = "Transparent, automated EVM network observatory with reproducible RPC, block and fee-market measurements."
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "khenzarr"}]

[tool.pytest.ini_options]
pythonpath = ["src"]
