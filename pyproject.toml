[project]
name = "chain-observatory"
version = "0.1.0"
description = "Transparent, automated EVM RPC observatory with reproducible hourly measurements."
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "khenzarr"}]

[tool.pytest.ini_options]
pythonpath = ["src"]
