[project]
name = "chain-observatory"
version = "0.3.0"
description = "Transparent multi-chain EVM observatory with reproducible RPC, block, fee-market, anomaly and health measurements."
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "khenzarr"}]

[tool.pytest.ini_options]
pythonpath = ["src"]
